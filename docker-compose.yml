services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-stats-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
